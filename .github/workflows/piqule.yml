name: Piqule

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:

  # ============================================================
  # Infra checks
  # ============================================================

  piqule-infra:
    name: Piqule / ${{ matrix.check }}
    runs-on: ubuntu-latest

    permissions:
      contents: read

    strategy:
      fail-fast: false
      matrix:
        check:
          - actionlint
          - hadolint
          - markdownlint
          - yamllint
          - typos
          - shellcheck
          - jsonlint

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2

      - name: Build Piqule Docker image
        run: |
          docker build \
            -f docker/Dockerfile \
            -t piqule .

      - name: Run ${{ matrix.check }}
        run: |
          docker run --rm \
            -v "$PWD:/project" \
            -w /project \
            piqule \
            check ${{ matrix.check }}

  # ============================================================
  # PHP-dependent checks
  # ============================================================

  piqule-php:
    name: Piqule / ${{ matrix.check }} (PHP ${{ matrix.php }})
    runs-on: ubuntu-latest

    permissions:
      contents: read

    strategy:
      fail-fast: false
      matrix:
        php: ["8.3", "8.5"]
        check:
          - phpcs
          - phpstan
          - psalm
          - phpmd
          - phpmetrics
          - phpunit
          - infection

    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2

      # ------------------------------------------------------------
      # Install project dependencies (required for phpunit/infection)
      # ------------------------------------------------------------
      - name: Install project dependencies
        run: composer install --no-interaction --no-progress

      - name: Build Piqule Docker image
        run: |
          docker build \
            --build-arg PHP_VERSION=${{ matrix.php }} \
            -f docker/Dockerfile \
            -t piqule .

      - name: Run ${{ matrix.check }}
        run: |
          docker run --rm \
            --user "$(id -u):$(id -g)" \
            -v "$PWD:/project" \
            -w /project \
            piqule \
            check ${{ matrix.check }}
