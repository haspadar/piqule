# Typos configuration
# Docs: https://github.com/crate-ci/typos

[default]
binary = false
extend-ignore-re = [
<< config(typos.ignore_re)|format_each('    "%s",')|join("\n") >>
]

[files]
extend-exclude = [
<< config(typos.exclude)|format_each('    "%s",')|join("\n") >>
]
