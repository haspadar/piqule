{
  "$schema": "../../vendor/infection/infection/resources/schema.json",

  "source": {
    "directories": [
<< config(infection.source.directories)
    |default_list(["../../src"])
    |format_each('        "%s"')
    |join(",")
>>
    ]
  },

  "testFramework": "phpunit",

  "phpUnit": {
    "configDir": "../phpunit"
  },

  "initialTestsPhpOptions": "<< config(infection.php_options)|default_scalar("-d memory_limit=1G") >>",
  "timeout": << config(infection.timeout)|default_scalar("30") >>,
  "logs": {
    "text": "infection.log",
    "summary": "infection-summary.log",
    "stryker": {
      "badge": "/^(main|develop)$/"
    }
  },

  "mutators": {
    "@default": true
  }
}
