<?xml version="1.0"?>
<ruleset name="Piqule PHPCS Rules">
    <description>Structural and semantic PHP rules</description>

    << config(phpcs.files)
    |default(["../../src"])
    |format("<file>%s</file>")
    |join("\n    ") >>

    << config(phpcs.excludes)
    |default(["vendor/*","tests/*"])
    |format("<exclude-pattern>%s</exclude-pattern>")
    |join("\n    ") >>

    <arg name="extensions" value="php"/>
    <arg name="colors"/>

    <rule ref="Generic.PHP.Syntax"/>
</ruleset>
