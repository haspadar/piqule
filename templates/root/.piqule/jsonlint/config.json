{
  "mode": << config(jsonlint.mode)|default_list(["json5"])|format_each('"%s"')|join("") >>,
  "compact": << config(jsonlint.compact)|default_scalar("true") >>,
  "continue": << config(jsonlint.continue)|default_scalar("true") >>,
  "duplicate-keys": << config(jsonlint.duplicate_keys)|default_scalar("false") >>,
  "patterns": [
<< config(jsonlint.patterns)
   |default_list([
      "**/*.json",
      "**/*.json5",
      "**/*.jsonc",
      "!**/vendor/**",
      "!**/node_modules/**",
      "!**/.git/**",
      "!**/coverage/**",
      "!**/build/**",
      "!**/var/**"
   ])
   |format_each('    "%s"')
   |join(",\n") >>
  ]
}
