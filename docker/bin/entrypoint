#!/usr/bin/env bash
set -euo pipefail

cmd="${1:-check}"

if [ $# -gt 0 ]; then
  shift
fi

case "$cmd" in
  check)
    exec /usr/local/lib/piqule/check "$@"
    ;;
  fix)
    exec /usr/local/lib/piqule/fix "$@"
    ;;
  *)
    echo "Unknown command: $cmd" >&2
    exit 1
    ;;
esac
