#!/usr/bin/env bash
set -euo pipefail

cmd="${1:-check}"
shift || true

case "$cmd" in
  check)
    exec /usr/local/lib/piqule/check "$@"
    ;;
  fix)
    exec /usr/local/lib/piqule/fix "$@"
    ;;
  *)
    echo "Unknown command: $cmd"
    exit 1
    ;;
esac
