# Typos configuration
# Docs: https://github.com/crate-ci/typos

[default]
binary = false
extend-ignore-re = [
    "vendor/.*",
]

[files]
extend-exclude = [
    ".git/",
    "vendor/",
    "node_modules/",
]